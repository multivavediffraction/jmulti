#version 400 core

in vec3 vertex;
in vec3 pos;
in vec3 color;
in float radius;

out vec3 vVertex;
out vec3 vPosition;
out vec3 vColor;
out float vRadius;

void main() {
    vVertex = normalize(vertex);
    vPosition = pos;
    vColor = color;
    vRadius = radius;
}
